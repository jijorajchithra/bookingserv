{
  "title": "Booking resource",
  "description": "Booking resource object",
  "type": "object",
  "properties": {
    "id": {
      "description": "Booking id",
      "type": "string"
    },
    "first_name": {
      "description": "First name",
      "type": "string",
      "minLength": 1,
      "maxLength": 255,
      "messages" : {
        "minLength": "client error: first name cannot be empty",
        "maxLength": "client error: first name cannot be greater than 255",
        "required": "client error: first name is required"
      }
    },
    "last_name": {
      "description": "Last name",
      "type": "string",
      "minLength": 1,
      "maxLength": 255
    },
    "address": {
      "$ref" : "address.json"
    }
  },
  "required": [
    "first_name",
    "last_name"
  ]
}